#include "main.h"
#include <stdio.h>
#include <ctype.h>
#include <stdlib.h>

/**
 * _abs - test for character or not
 * @n: the character to be tested
 * Desciption: test for alphanumeric
 * Return: On success 1 , 0 and -1.
 */

int _abs(int n)
{
	return (abs(n));
}
